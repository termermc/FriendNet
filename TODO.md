# Documentation

Make a docs site.

Embed that site in the client once it's made, since the client is just a web UI anyway.

# Update Checker

Make an update checker (not auto-updater) that periodically checks for updates.
It must validate that updates (both the downloads and the update notifications) are signed by a maintainer.

# Misc

Put client RPC on same HTTP server as web UI.
